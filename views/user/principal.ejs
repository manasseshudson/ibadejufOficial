<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Painel do Aluno - IBADEJUF</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    .progress-bar {
      height: 6px;
      border-radius: 9999px;
      overflow: hidden;
      background-color: #e5e7eb;
    }
    .progress-fill {
      height: 100%;
      border-radius: 9999px;
      transition: width 0.5s ease;
    }
  </style>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">

  <!-- Sidebar -->
  <div id="sidebar" class="fixed top-0 left-0 h-full w-64 bg-white shadow-lg z-50 transform -translate-x-full transition-transform duration-300 ease-in-out lg:translate-x-0">
    <div class="p-6 border-b border-gray-200 flex items-center space-x-2">
      <i class="fas fa-graduation-cap text-blue-600 text-xl"></i>
      <h1 class="text-xl font-bold text-blue-600">IBADEJUF</h1>
    </div>

    <nav class="mt-8 px-4">
      <ul class="space-y-4">
        <li><a href="/user/principal/<%= uid_usuario %>" class="flex items-center space-x-3 p-3 rounded-lg hover:bg-blue-50 text-gray-700 hover:text-blue-600 transition"><i class="fas fa-home"></i> <span>Principal</span></a></li>
        <li><a href="/user/materias/<%= uid_usuario %>" class="flex items-center space-x-3 p-3 rounded-lg hover:bg-blue-50 text-gray-700 hover:text-blue-600 transition"><i class="fas fa-book-open"></i> <span>Matérias</span></a></li>
        <li><a href="/user/profile/<%= uid_usuario %>" class="flex items-center space-x-3 p-3 rounded-lg hover:bg-blue-50 text-gray-700 hover:text-blue-600 transition"><i class="fas fa-user"></i> <span>Dados Pessoais</span></a></li>
        <li><a href="/loginv2" class="flex items-center space-x-3 p-3 rounded-lg hover:bg-red-50 text-gray-700 hover:text-red-600 transition"><i class="fas fa-sign-out-alt"></i> <span>Sair</span></a></li>
      </ul>
    </nav>
  </div>

  <!-- Main Content -->
  <div class="lg:ml-64 transition-all duration-300">
    <!-- Header -->
    <header class="bg-blue-600 text-white p-4 shadow-md flex justify-between items-center">
      <button id="menuToggle" class="lg:hidden p-2 rounded-md hover:bg-blue-700">
        <i class="fas fa-bars text-xl"></i>
      </button>
      <h1 class="text-xl font-bold">IBADEJUF - Painel do Aluno</h1>
      <div class="hidden lg:block w-10"></div> <!-- Espaço para alinhar -->
    </header>

    <!-- Welcome Section -->
    <section class="p-6">
      <div class="bg-white rounded-lg shadow-sm p-4 mb-6">
        <h2 class="text-lg font-semibold">Olá MANASSES HUDSON Cdd, seja bem-vindo!</h2>
      </div>

      <!-- Stats Cards -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <!-- Matérias -->
        <div class="bg-white rounded-xl shadow p-5 hover:shadow-lg transition">
          <h3 class="text-sm font-medium text-gray-500 mb-1">Matérias Disponíveis</h3>
          <div class="flex items-end justify-between">
            <span class="text-3xl font-bold text-blue-600"><%= qtdeMateriasDisponiveis %></span>
            <span class="text-xs text-gray-400">Matérias</span>
          </div>
          <div class="progress-bar mt-3">
            <div class="progress-fill bg-blue-600" style="width: 80%"></div>
          </div>
        </div>

        <!-- Aulas Disponíveis -->
        <div class="bg-white rounded-xl shadow p-5 hover:shadow-lg transition">
          <h3 class="text-sm font-medium text-gray-500 mb-1">Aulas Disponíveis</h3>
          <div class="flex items-end justify-between">
            <span class="text-3xl font-bold text-purple-600"><%= qtdeAulasDisponiveis %></span>
            <span class="text-xs text-gray-400">Aulas</span>
          </div>
          <div class="progress-bar mt-3">
            <div class="progress-fill bg-purple-600" style="width: 70%"></div>
          </div>
        </div>

        <!-- Aulas Assistidas -->
        <div class="bg-white rounded-xl shadow p-5 hover:shadow-lg transition">
          <h3 class="text-sm font-medium text-gray-500 mb-1">Aulas Assistidas</h3>
          <div class="flex items-end justify-between">
            <span class="text-3xl font-bold text-green-600"><%= qtdeAulasAssistidas %></span>
            <span class="text-xs text-gray-400">Aulas</span>
          </div>
          <div class="progress-bar mt-3">
            <div class="progress-fill bg-green-600" style="width: 17%"></div>
          </div>
        </div>
      </div>

      <!-- Course Info Card -->
      <div class="bg-gray-100 rounded-xl shadow p-6 mb-8 flex flex-col md:flex-row items-center gap-6">
        <div class="w-full md:w-1/3">
          <img src="https://campustechnology.com/-/media/EDU/CampusTechnology/2021-images/20210121collegestudents.jpg" alt="Estudantes" class="rounded-lg shadow-md w-full" />
        </div>
        <div class="w-full md:w-2/3">
          <h2 class="text-2xl font-extrabold text-gray-800 mb-2">CURSO DE TEOLOGIA ONLINE IBADEJUF</h2>
          <p class="text-sm text-gray-500 mb-4">CURSO BÁSICO + MÉDIO EM TEOLOGIA</p>
          <p class="mb-4 text-gray-700 leading-relaxed">
            Agora com uma nova e exclusiva metodologia na qual você aprende muito mais e conquista o seu Diploma estudando 100% Online.
          </p>
		  <% if(modulo=="2"){ %>
          <div class="mt-6">
            <h3 class="text-xl font-bold text-gray-800 mb-2">PROVA DISPONÍVEL</h3>
            <p class="text-sm text-gray-500 mb-4">PROVA DE ESCATOLOGIA</p>
            <button id="takeTestBtn" class="bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-6 rounded-lg transition shadow-md flex items-center space-x-2">
              <i class="fas fa-clipboard-check"></i>
              <a href="https://docs.google.com/forms/d/e/1FAIpQLSeG2046D7Fxbu3ZBW2wiesA0-7qCI2HqhOO_vmdL7P_WKgodw/viewform?usp=dialog" target="_blanck"><span>Clique aqui para realizar a prova</span></a>
            </button>
          </div>
		  <% } %>
        </div>
      </div>
    </section>
  </div>

  <!-- Modal (opcional) -->
  <div id="testModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
    <div class="bg-white rounded-xl p-6 max-w-md w-full mx-4 shadow-xl">
      <h3 class="text-xl font-bold mb-4">Iniciar Prova de Escatologia?</h3>
      <p class="mb-6 text-gray-600">Você tem 30 minutos para completar a prova. Boa sorte!</p>
      <div class="flex justify-end space-x-3">
        <button id="cancelTest" class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-100">Cancelar</button>
        <button id="confirmTest" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">Iniciar Prova</button>
      </div>
    </div>
  </div>

  <script>
    // Toggle Sidebar
    document.getElementById('menuToggle').addEventListener('click', () => {
      const sidebar = document.getElementById('sidebar');
      sidebar.classList.toggle('-translate-x-full');
    });

    // Test Button Modal
    //const takeTestBtn = document.getElementById('takeTestBtn');
    const testModal = document.getElementById('testModal');
    const cancelTest = document.getElementById('cancelTest');
    const confirmTest = document.getElementById('confirmTest');

    /*takeTestBtn.addEventListener('click', () => {
      testModal.classList.remove('hidden');
    });*/

    cancelTest.addEventListener('click', () => {
      testModal.classList.add('hidden');
    });

    confirmTest.addEventListener('click', () => {
      alert("Prova iniciada! Redirecionando...");
      // window.location.href = "/prova"; // Descomente para redirecionar
      testModal.classList.add('hidden');
    });
  </script>

</body>
</html>