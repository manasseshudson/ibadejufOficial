<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IBADEJUF - Cursos Online de Teologia</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, #1e3a8a 0%, #3730a3 50%, #581c87 100%);
        }
        
        .gradient-text {
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #a855f7 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .floating-element {
            animation: float 8s ease-in-out infinite;
        }
        
        .floating-element:nth-child(2) {
            animation-delay: -2s;
        }
        
        .floating-element:nth-child(3) {
            animation-delay: -4s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            33% { transform: translateY(-15px) rotate(0.5deg); }
            66% { transform: translateY(-8px) rotate(-0.5deg); }
        }
        
        .scroll-reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }
        
        .scroll-reveal.revealed {
            opacity: 1;
            transform: translateY(0);
        }
        
        .glass-effect {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .hover-lift {
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }
        
        .hover-lift:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }
        
        .course-card {
            background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
            border: 1px solid rgba(226, 232, 240, 0.8);
            transition: all 0.3s ease;
        }
        
        .course-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(30, 58, 138, 0.15);
            border-color: rgba(59, 130, 246, 0.3);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
        }
        
        .section-divider {
            background: linear-gradient(90deg, transparent, #e5e7eb, transparent);
            height: 1px;
        }
        
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body class="bg-gray-50 overflow-x-hidden">
    <!-- Navigation -->
    <nav class="fixed top-0 w-full z-50 glass-effect">
        <div class="max-w-7xl mx-auto px-6 py-4">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-gradient-to-br from-blue-600 to-purple-600 rounded-lg flex items-center justify-center">
                        <span class="text-white font-bold text-lg">IB</span>
                    </div>
                    <div>
                        <div class="text-xl font-bold text-white">IBADEJUF</div>
                        <div class="text-xs text-blue-200">Instituto Bíblico</div>
                    </div>
                </div>
                <div class="hidden md:flex space-x-8">
                    <a href="#cursos" class="text-white hover:text-blue-200 transition-colors">Cursos</a>
                    <a href="#sobre" class="text-white hover:text-blue-200 transition-colors">Sobre</a>
                    <a href="#contato" class="text-white hover:text-blue-200 transition-colors">Contato</a>
                </div>
                <div class="flex space-x-3">
                    <button id="areaAlunoBtn" class="border border-white/30 text-white px-6 py-2 rounded-full font-semibold hover:bg-white/10 transition-colors">
                        Área do Aluno
                    </button>
                    <!--<button id="inscricaoBtn" class="bg-white text-blue-600 px-6 py-2 rounded-full font-semibold hover:bg-blue-50 transition-colors">
                        Inscreva-se
                    </button>-->
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="relative min-h-screen flex items-center justify-center gradient-bg overflow-hidden">
        <!-- Floating Elements -->
        <div class="absolute inset-0 overflow-hidden">
            <div class="floating-element absolute top-20 left-10 w-64 h-64 bg-gradient-to-br from-blue-400 to-purple-400 rounded-full opacity-20 blur-2xl"></div>
            <div class="floating-element absolute top-40 right-20 w-80 h-80 bg-gradient-to-br from-purple-400 to-pink-400 rounded-full opacity-20 blur-2xl"></div>
            <div class="floating-element absolute bottom-20 left-1/3 w-72 h-72 bg-gradient-to-br from-indigo-400 to-blue-400 rounded-full opacity-20 blur-2xl"></div>
        </div>
        
        <div class="relative z-10 text-center px-6 max-w-6xl mx-auto text-white">
            <div class="scroll-reveal">
                <div class="inline-flex items-center space-x-2 bg-white/20 rounded-full px-6 py-2 mb-8">
                    <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
                    <span class="text-sm font-medium">Matrículas Abertas 2025</span>
                </div>
            </div>
            
            <div class="scroll-reveal">
                <h1 class="text-5xl md:text-7xl font-black leading-tight mb-6">
                    Transforme sua<br>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-orange-300">
                        Vida Espiritual
                    </span>
                </h1>
            </div>
            
            <div class="scroll-reveal" style="transition-delay: 0.2s;">
                <p class="text-xl md:text-2xl text-blue-100 mb-12 max-w-4xl mx-auto leading-relaxed">
                    Cursos Online de Teologia do IBADEJUF. Formação sólida, flexível e acessível para capacitar líderes e servos de Deus.
                </p>
            </div>
            
            <div class="scroll-reveal flex flex-col sm:flex-row gap-6 justify-center items-center" style="transition-delay: 0.4s;">
                <button class="btn-primary text-white px-10 py-4 rounded-full text-lg font-semibold">
                    Ver Cursos Disponíveis
                </button>
                <button class="border-2 border-white/30 text-white px-10 py-4 rounded-full text-lg font-semibold hover:bg-white/10 transition-colors">
                    Saiba Mais
                </button>
            </div>
            
            <!-- Stats 
            <div class="scroll-reveal grid grid-cols-2 md:grid-cols-4 gap-8 mt-20 max-w-4xl mx-auto" style="transition-delay: 0.6s;">
                <div class="text-center">
                    <div class="text-3xl md:text-4xl font-bold text-yellow-300 mb-2">500+</div>
                    <div class="text-blue-200 text-sm">Alunos Formados</div>
                </div>
                <div class="text-center">
                    <div class="text-3xl md:text-4xl font-bold text-yellow-300 mb-2">15+</div>
                    <div class="text-blue-200 text-sm">Anos de Experiência</div>
                </div>
                <div class="text-center">
                    <div class="text-3xl md:text-4xl font-bold text-yellow-300 mb-2">4</div>
                    <div class="text-blue-200 text-sm">Cursos Disponíveis</div>
                </div>
                <div class="text-center">
                    <div class="text-3xl md:text-4xl font-bold text-yellow-300 mb-2">100%</div>
                    <div class="text-blue-200 text-sm">Online</div>
                </div>
            </div>-->
        </div>
        
        <!-- Scroll Indicator -->
        <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
            <div class="w-6 h-10 border-2 border-white/50 rounded-full flex justify-center">
                <div class="w-1 h-3 bg-white/70 rounded-full mt-2 animate-pulse"></div>
            </div>
        </div>
    </section>

    <!-- Video Preview Section -->
    <section class="py-24 bg-gradient-to-br from-gray-50 to-blue-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="text-center mb-16">
                <div class="scroll-reveal">
                    <h2 class="text-5xl md:text-6xl font-black text-gray-900 mb-6">
                        Conheça <span class="gradient-text">Nossa Metodologia</span>
                    </h2>
                </div>
                <div class="scroll-reveal" style="transition-delay: 0.2s;">
                    <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                        Veja como funcionam nossos cursos e qual a estrutura de ensino do IBADEJUF
                    </p>
                </div>
                <div class="section-divider w-24 mx-auto mt-8"></div>
            </div>
            
            <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
                <!-- Video 1 - Apresentação -->
                <div class="scroll-reveal bg-white rounded-3xl shadow-2xl p-6 hover-lift cursor-pointer video-preview" data-video="193gnJ1FQ8s" data-title="Apresentação do IBADEJUF">
                    <div class="aspect-video bg-gray-100 rounded-2xl flex items-center justify-center mb-4 relative overflow-hidden">
                        <img 
                            src="https://i.ytimg.com/vi/193gnJ1FQ8s/maxresdefault.jpg" 
                            alt="Apresentação do IBADEJUF"
                            class="w-full h-full object-cover"
                        >
                        <div class="absolute inset-0 bg-black/30 flex items-center justify-center hover:bg-black/40 transition-colors">
                            <div class="w-16 h-16 bg-white/90 rounded-full flex items-center justify-center shadow-lg">
                                <span class="text-blue-600 text-2xl ml-1">▶</span>
                            </div>
                        </div>
                    </div>
                    <div class="text-center">
                        <h3 class="text-lg font-bold text-gray-900 mb-2">Apresentação Institucional</h3>
                        <p class="text-gray-600 text-sm">Conheça a história e missão do IBADEJUF</p>
                    </div>
                </div>

                <!-- Video 2 - Metodologia -->
                <div class="scroll-reveal bg-white rounded-3xl shadow-2xl p-6 hover-lift cursor-pointer video-preview" data-video="193gnJ1FQ8s" data-title="Nossa Metodologia de Ensino" style="transition-delay: 0.1s;">
                    <div class="aspect-video bg-gray-100 rounded-2xl flex items-center justify-center mb-4 relative overflow-hidden">
                        <img 
                            src="https://i.ytimg.com/vi/193gnJ1FQ8s/maxresdefault.jpg" 
                            alt="Metodologia de Ensino"
                            class="w-full h-full object-cover"
                        >
                        <div class="absolute inset-0 bg-black/30 flex items-center justify-center hover:bg-black/40 transition-colors">
                            <div class="w-16 h-16 bg-white/90 rounded-full flex items-center justify-center shadow-lg">
                                <span class="text-green-600 text-2xl ml-1">▶</span>
                            </div>
                        </div>
                    </div>
                    <div class="text-center">
                        <h3 class="text-lg font-bold text-gray-900 mb-2">Metodologia de Ensino</h3>
                        <p class="text-gray-600 text-sm">Como funcionam nossas aulas e avaliações</p>
                    </div>
                </div>

                <!-- Video 3 - Depoimentos -->
                <div class="scroll-reveal bg-white rounded-3xl shadow-2xl p-6 hover-lift cursor-pointer video-preview" data-video="193gnJ1FQ8s" data-title="Depoimentos de Alunos" style="transition-delay: 0.2s;">
                    <div class="aspect-video bg-gray-100 rounded-2xl flex items-center justify-center mb-4 relative overflow-hidden">
                        <img 
                            src="https://i.ytimg.com/vi/193gnJ1FQ8s/maxresdefault.jpg" 
                            alt="Depoimentos de Alunos"
                            class="w-full h-full object-cover"
                        >
                        <div class="absolute inset-0 bg-black/30 flex items-center justify-center hover:bg-black/40 transition-colors">
                            <div class="w-16 h-16 bg-white/90 rounded-full flex items-center justify-center shadow-lg">
                                <span class="text-purple-600 text-2xl ml-1">▶</span>
                            </div>
                        </div>
                    </div>
                    <div class="text-center">
                        <h3 class="text-lg font-bold text-gray-900 mb-2">Depoimentos de Alunos</h3>
                        <p class="text-gray-600 text-sm">Histórias de transformação e crescimento</p>
                    </div>
                </div>

                <!-- Video 4 - Campus Virtual -->
                <div class="scroll-reveal bg-white rounded-3xl shadow-2xl p-6 hover-lift cursor-pointer video-preview" data-video="193gnJ1FQ8s" data-title="Tour pelo Campus Virtual" style="transition-delay: 0.3s;">
                    <div class="aspect-video bg-gray-100 rounded-2xl flex items-center justify-center mb-4 relative overflow-hidden">
                        <img 
                            src="https://i.ytimg.com/vi/193gnJ1FQ8s/maxresdefault.jpg" 
                            alt="Campus Virtual"
                            class="w-full h-full object-cover"
                        >
                        <div class="absolute inset-0 bg-black/30 flex items-center justify-center hover:bg-black/40 transition-colors">
                            <div class="w-16 h-16 bg-white/90 rounded-full flex items-center justify-center shadow-lg">
                                <span class="text-orange-600 text-2xl ml-1">▶</span>
                            </div>
                        </div>
                    </div>
                    <div class="text-center">
                        <h3 class="text-lg font-bold text-gray-900 mb-2">Tour pelo Campus Virtual</h3>
                        <p class="text-gray-600 text-sm">Conheça nossa plataforma de ensino online</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Courses Section -->
    <section id="cursos" class="py-24 bg-white">
        <div class="max-w-7xl mx-auto px-6">
            <div class="text-center mb-16">
                <div class="scroll-reveal">
                    <h2 class="text-5xl md:text-6xl font-black text-gray-900 mb-6">
                        Nossos <span class="gradient-text">Cursos</span>
                    </h2>
                </div>
                <div class="scroll-reveal" style="transition-delay: 0.2s;">
                    <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                        Formação teológica completa e progressiva, do básico ao avançado
                    </p>
                </div>
                <div class="section-divider w-24 mx-auto mt-8"></div>
            </div>
            
            <div class="grid md:grid-cols-2 lg:grid-cols-2 gap-8 max-w-6xl mx-auto">
                <!-- Curso 1º Básico -->
                <div class="scroll-reveal course-card rounded-2xl p-8 hover-lift">
                    <div class="flex items-center justify-between mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-emerald-600 rounded-xl flex items-center justify-center">
                            <span class="text-white font-bold text-xl">1º</span>
                        </div>
                        <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-semibold">Básico</span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-4">1º Básico em Teologia</h3>
                    <p class="text-gray-600 mb-6 leading-relaxed">
                        Fundamentos bíblicos essenciais. Ideal para iniciantes que desejam conhecer os princípios básicos da fé cristã e da interpretação bíblica.
                    </p>
                    <ul class="space-y-3 mb-8">
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-green-500 rounded-full mr-3"></div>
                            Introdução à Bíblia
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-green-500 rounded-full mr-3"></div>
                            Doutrinas Fundamentais
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-green-500 rounded-full mr-3"></div>
                            História do Cristianismo
                        </li>
                    </ul>
                    <button class="w-full bg-green-600 text-white py-3 rounded-xl font-semibold hover:bg-green-700 transition-colors">
                        Matricular-se
                    </button>
                </div>

                <!-- Curso 2º Básico -->
                <div class="scroll-reveal course-card rounded-2xl p-8 hover-lift" style="transition-delay: 0.1s;">
                    <div class="flex items-center justify-between mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-xl flex items-center justify-center">
                            <span class="text-white font-bold text-xl">2º</span>
                        </div>
                        <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-semibold">Básico</span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-4">2º Básico em Teologia</h3>
                    <p class="text-gray-600 mb-6 leading-relaxed">
                        Aprofundamento dos conhecimentos bíblicos. Continuação natural do 1º Básico com temas mais elaborados e práticos.
                    </p>
                    <ul class="space-y-3 mb-8">
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-blue-500 rounded-full mr-3"></div>
                            Hermenêutica Bíblica
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-blue-500 rounded-full mr-3"></div>
                            Teologia Sistemática
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-blue-500 rounded-full mr-3"></div>
                            Apologética Cristã
                        </li>
                    </ul>
                    <button class="w-full bg-blue-600 text-white py-3 rounded-xl font-semibold hover:bg-blue-700 transition-colors">
                        Matricular-se
                    </button>
                </div>

                <!-- Curso Médio -->
                <div class="scroll-reveal course-card rounded-2xl p-8 hover-lift" style="transition-delay: 0.2s;">
                    <div class="flex items-center justify-between mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-xl flex items-center justify-center">
                            <span class="text-white font-bold text-lg">M</span>
                        </div>
                        <span class="bg-purple-100 text-purple-800 px-3 py-1 rounded-full text-sm font-semibold">Intermediário</span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-4">Médio em Teologia</h3>
                    <p class="text-gray-600 mb-6 leading-relaxed">
                        Formação intermediária completa. Para quem deseja um conhecimento mais profundo e está considerando o ministério.
                    </p>
                    <ul class="space-y-3 mb-8">
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-purple-500 rounded-full mr-3"></div>
                            Exegese Bíblica
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-purple-500 rounded-full mr-3"></div>
                            Homilética
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-purple-500 rounded-full mr-3"></div>
                            Liderança Cristã
                        </li>
                    </ul>
                    <button class="w-full bg-purple-600 text-white py-3 rounded-xl font-semibold hover:bg-purple-700 transition-colors">
                        Matricular-se
                    </button>
                </div>

                <!-- Curso Diáconos -->
                <div class="scroll-reveal course-card rounded-2xl p-8 hover-lift" style="transition-delay: 0.3s;">
                    <div class="flex items-center justify-between mb-6">
                        <div class="w-16 h-16 bg-gradient-to-br from-orange-500 to-red-600 rounded-xl flex items-center justify-center">
                            <span class="text-white font-bold text-lg">D</span>
                        </div>
                        <span class="bg-orange-100 text-orange-800 px-3 py-1 rounded-full text-sm font-semibold">Especialização</span>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-4">Diáconos e Auxiliares</h3>
                    <p class="text-gray-600 mb-6 leading-relaxed">
                        Formação específica para o ministério diaconal. Capacitação prática para servir com excelência na casa de Deus.
                    </p>
                    <ul class="space-y-3 mb-8">
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-orange-500 rounded-full mr-3"></div>
                            Ministério Diaconal
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-orange-500 rounded-full mr-3"></div>
                            Aconselhamento Pastoral
                        </li>
                        <li class="flex items-center text-gray-700">
                            <div class="w-2 h-2 bg-orange-500 rounded-full mr-3"></div>
                            Administração Eclesiástica
                        </li>
                    </ul>
                    <button class="w-full bg-orange-600 text-white py-3 rounded-xl font-semibold hover:bg-orange-700 transition-colors">
                        Matricular-se
                    </button>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="sobre" class="py-24 bg-gradient-to-br from-slate-50 to-blue-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="grid lg:grid-cols-2 gap-16 items-center">
                <div class="scroll-reveal">
                    <h2 class="text-5xl font-black text-gray-900 mb-8">
                        Sobre o <span class="gradient-text">IBADEJUF</span>
                    </h2>
                    <p class="text-lg text-gray-600 mb-8 leading-relaxed">
                        O Instituto Bíblico da Assembleia de Deus de Juiz de Fora tem mais de 15 anos de experiência na formação teológica. Nossa missão é capacitar homens e mulheres para o serviço cristão através de uma educação bíblica sólida e prática.
                    </p>
                    <div class="grid md:grid-cols-2 gap-6 mb-8">
                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0 mt-1">
                                <span class="text-blue-600 text-sm">✓</span>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900 mb-1">Professores Qualificados</h4>
                                <p class="text-gray-600 text-sm">Corpo docente experiente e capacitado</p>
                            </div>
                        </div>
                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0 mt-1">
                                <span class="text-blue-600 text-sm">✓</span>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900 mb-1">Certificação Reconhecida</h4>
                                <p class="text-gray-600 text-sm">Certificados validados e reconhecidos</p>
                            </div>
                        </div>
                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0 mt-1">
                                <span class="text-blue-600 text-sm">✓</span>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900 mb-1">Flexibilidade Total</h4>
                                <p class="text-gray-600 text-sm">Estude no seu ritmo e horário</p>
                            </div>
                        </div>
                        <div class="flex items-start space-x-3">
                            <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center flex-shrink-0 mt-1">
                                <span class="text-blue-600 text-sm">✓</span>
                            </div>
                            <div>
                                <h4 class="font-semibold text-gray-900 mb-1">Suporte Contínuo</h4>
                                <p class="text-gray-600 text-sm">Acompanhamento durante todo o curso</p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="scroll-reveal" style="transition-delay: 0.2s;">
                    <div class="bg-white rounded-3xl shadow-2xl p-8 hover-lift">
                        <div class="aspect-video bg-gradient-to-br from-blue-100 to-purple-100 rounded-2xl flex items-center justify-center mb-6">
                            <div class="text-center">
                                <div class="w-16 h-16 bg-gradient-to-br from-blue-600 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-4">
                                    <span class="text-white text-2xl">🎓</span>
                                </div>
                                <p class="text-gray-600">Campus Virtual IBADEJUF</p>
                            </div>
                        </div>
                        <div class="text-center">
                            <h3 class="text-xl font-bold text-gray-900 mb-2">Plataforma Moderna</h3>
                            <p class="text-gray-600">Interface intuitiva e recursos avançados de aprendizagem</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contato" class="py-24 bg-white">
        <div class="max-w-4xl mx-auto px-6">
            <div class="text-center mb-16">
                <div class="scroll-reveal">
                    <h2 class="text-5xl md:text-6xl font-black text-gray-900 mb-6">
                        Fale <span class="gradient-text">Conosco</span>
                    </h2>
                </div>
                <div class="scroll-reveal" style="transition-delay: 0.2s;">
                    <p class="text-xl text-gray-600">
                        Tire suas dúvidas ou solicite mais informações sobre nossos cursos
                    </p>
                </div>
                <div class="section-divider w-24 mx-auto mt-8"></div>
            </div>
            
            <div class="scroll-reveal bg-gradient-to-br from-gray-50 to-blue-50 rounded-3xl shadow-xl p-8 md:p-12 hover-lift" style="transition-delay: 0.4s;">
                <form class="space-y-6">
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-3">Nome Completo</label>
                            <input type="text" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors">
                        </div>
                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-3">WhatsApp</label>
                            <input type="tel" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors">
                        </div>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">E-mail</label>
                        <input type="email" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors">
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">Curso de Interesse</label>
                        <select class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors">
                            <option value="">Selecione um curso</option>
                            <option value="1-basico">1º Básico em Teologia</option>
                            <option value="2-basico">2º Básico em Teologia</option>
                            <option value="medio">Médio em Teologia</option>
                            <option value="diaconos">Curso para Diáconos e Auxiliares</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">Mensagem</label>
                        <textarea rows="5" class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors resize-none" placeholder="Conte-nos mais sobre seu interesse..."></textarea>
                    </div>
                    
                    <button type="submit" class="w-full btn-primary text-white px-8 py-4 rounded-xl text-lg font-semibold">
                        Enviar Mensagem
                    </button>
                </form>
                
                <!-- Contact Info -->
                <div class="grid md:grid-cols-3 gap-6 mt-12 pt-8 border-t border-gray-200">
                    <div class="text-center">
                        <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="text-blue-600 text-xl">📱</span>
                        </div>
                        <h4 class="font-semibold text-gray-900 mb-1">WhatsApp</h4>
                        <p class="text-gray-600 text-sm">(32) 99999-9999</p>
                    </div>
                    <div class="text-center">
                        <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="text-blue-600 text-xl">✉️</span>
                        </div>
                        <h4 class="font-semibold text-gray-900 mb-1">E-mail</h4>
                        <p class="text-gray-600 text-sm">ibadejuf@gmail.com</p>
                    </div>
                    <div class="text-center">
                        <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="text-blue-600 text-xl">📍</span>
                        </div>
                        <h4 class="font-semibold text-gray-900 mb-1">Endereço</h4>
                        <p class="text-gray-600 text-sm">Juiz de Fora - MG</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="py-16 gradient-bg text-white">
        <div class="max-w-7xl mx-auto px-6">
            <div class="text-center">
                <div class="flex items-center justify-center space-x-3 mb-6">
                    <div class="w-12 h-12 bg-white/20 rounded-xl flex items-center justify-center">
                        <span class="text-white font-bold text-xl">IB</span>
                    </div>
                    <div>
                        <div class="text-2xl font-bold">IBADEJUF</div>
                        <div class="text-sm text-blue-200">Instituto Bíblico</div>
                    </div>
                </div>
                <p class="text-blue-200 mb-8 max-w-2xl mx-auto">
                    Capacitando servos de Deus através da educação bíblica de qualidade há mais de 15 anos.
                </p>
                <div class="flex justify-center space-x-8 mb-8">
                    <a href="#cursos" class="text-blue-200 hover:text-white transition-colors">Cursos</a>
                    <a href="#sobre" class="text-blue-200 hover:text-white transition-colors">Sobre</a>
                    <a href="#contato" class="text-blue-200 hover:text-white transition-colors">Contato</a>
                </div>
                <div class="text-blue-300 text-sm">
                    © 2025 IBADEJUF. Transformando vidas através da Palavra.
                </div>
            </div>
        </div>
    </footer>

    <!-- Modal de Vídeo -->
    <div id="videoModal" class="fixed inset-0 bg-black bg-opacity-80 backdrop-blur-sm z-50 hidden items-center justify-center p-4 overflow-y-auto">
        <div class="bg-white rounded-3xl shadow-2xl max-w-4xl w-full my-8 flex flex-col" style="max-height: calc(100vh - 4rem);">
            <!-- Header do Modal -->
            <div class="flex justify-between items-center p-6 border-b border-gray-200 flex-shrink-0">
                <h3 id="videoTitle" class="text-lg font-bold text-gray-900">Vídeo</h3>
                <button id="closeVideoModal" class="w-10 h-10 bg-gray-100 rounded-full flex items-center justify-center hover:bg-gray-200 transition-colors">
                    <span class="text-gray-600 text-xl">×</span>
                </button>
            </div>
            
            <!-- Conteúdo do Vídeo -->
            <div class="p-6 flex-1 overflow-y-auto">
                <div class="aspect-video bg-black rounded-2xl overflow-hidden mb-6">
                    <iframe 
                        id="youtubeFrame"
                        width="100%" 
                        height="100%" 
                        src="" 
                        title="YouTube video player" 
                        frameborder="0" 
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                        allowfullscreen>
                    </iframe>
                </div>
                
                <div class="text-center">
                    <p class="text-gray-600 mb-4">
                        Gostou do que viu? <strong>Inscreva-se agora</strong> e faça parte da nossa comunidade de estudos!
                    </p>
                    <button id="inscrevaSeVideo" class="btn-primary text-white px-8 py-3 rounded-full font-semibold">
                        Quero me Inscrever
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Área do Aluno -->
    <div id="areaAlunoModal" class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
        <div class="bg-white rounded-3xl shadow-2xl max-w-lg w-full">
            <div class="p-8">
                <!-- Header do Modal -->
                <div class="flex justify-between items-center mb-8">
                    <div>
                        <h3 class="text-2xl font-bold text-gray-900 mb-2">Área do Aluno</h3>
                        <p class="text-gray-600">Selecione sua área de estudos</p>
                    </div>
                    <button id="closeAreaAlunoModal" class="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center hover:bg-gray-200 transition-colors">
                        <span class="text-gray-600 text-lg">×</span>
                    </button>
                </div>

                <!-- Opções de Cursos -->
                <div class="space-y-4">
                    <!-- IBADEJUF -->
                    <div class="bg-gradient-to-br from-blue-50 to-purple-50 rounded-2xl p-6 hover:shadow-lg transition-all cursor-pointer border-2 border-transparent hover:border-blue-200" onclick="acessarAreaAluno('ibadejuf')">
                        <div class="flex items-center space-x-4">
                            <div class="w-16 h-16 bg-gradient-to-br from-blue-600 to-purple-600 rounded-xl flex items-center justify-center flex-shrink-0">
                                <span class="text-white font-bold text-xl">IB</span>
                            </div>
                            <div class="flex-1">
                                <h4 class="text-xl font-bold text-gray-900 mb-2">Cursos IBADEJUF</h4>
                                <p class="text-gray-600 text-sm mb-3">Formação em Teologia - Básico e Médio</p>
                                <div class="flex flex-wrap gap-2">
                                    <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded-full text-xs font-medium">1º Básico</span>
                                    <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded-full text-xs font-medium">2º Básico</span>
                                    <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded-full text-xs font-medium">Médio</span>
                                </div>
                            </div>
                            <div class="text-blue-600">
                                <span class="text-2xl">→</span>
                            </div>
                        </div>
                    </div>

                    <!-- Diáconos -->
                    <div class="bg-gradient-to-br from-orange-50 to-red-50 rounded-2xl p-6 hover:shadow-lg transition-all cursor-pointer border-2 border-transparent hover:border-orange-200" onclick="acessarAreaAluno('diaconos')">
                        <div class="flex items-center space-x-4">
                            <div class="w-16 h-16 bg-gradient-to-br from-orange-600 to-red-600 rounded-xl flex items-center justify-center flex-shrink-0">
                                <span class="text-white font-bold text-xl">D</span>
                            </div>
                            <div class="flex-1">
                                <h4 class="text-xl font-bold text-gray-900 mb-2">Curso para Diáconos</h4>
                                <p class="text-gray-600 text-sm mb-3">Formação específica para o ministério diaconal</p>
                                <div class="flex flex-wrap gap-2">
                                    <span class="bg-orange-100 text-orange-800 px-2 py-1 rounded-full text-xs font-medium">Ministério Diaconal</span>
                                    <span class="bg-orange-100 text-orange-800 px-2 py-1 rounded-full text-xs font-medium">Aconselhamento</span>
                                </div>
                            </div>
                            <div class="text-orange-600">
                                <span class="text-2xl">→</span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Informações Adicionais --><!--
                <div class="mt-8 bg-gray-50 rounded-xl p-4">
                    <div class="flex items-start space-x-3">
                        <div class="w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                            <span class="text-blue-600 text-sm">ℹ</span>
                        </div>
                        <div>
                            <h5 class="font-semibold text-gray-900 mb-1">Acesso para Alunos Matriculados</h5>
                            <p class="text-gray-600 text-sm">
                                Para acessar a área do aluno, você precisa estar matriculado em um dos nossos cursos. 
                                Após a seleção, você será direcionado para o login da plataforma correspondente.
                            </p>
                        </div>
                    </div>
                </div>-->

                <!-- Botão de Fechar -->
                <div class="mt-6">
                    <button 
                        id="cancelarAreaAluno"
                        class="w-full border-2 border-gray-200 text-gray-700 py-3 rounded-xl font-semibold hover:bg-gray-50 transition-colors"
                    >
                        Fechar
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal de Inscrição -->
    <div id="inscricaoModal" class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
        <div class="bg-white rounded-3xl shadow-2xl max-w-md w-full max-h-[90vh] overflow-y-auto">
            <div class="p-8">
                <!-- Header do Modal -->
                <div class="flex justify-between items-center mb-6">
                    <div>
                        <h3 class="text-2xl font-bold text-gray-900 mb-2">Inscreva-se</h3>
                        <p class="text-gray-600">Preencha seus dados para iniciar sua jornada</p>
                    </div>
                    <button id="closeModal" class="w-8 h-8 bg-gray-100 rounded-full flex items-center justify-center hover:bg-gray-200 transition-colors">
                        <span class="text-gray-600 text-lg">×</span>
                    </button>
                </div>

                <!-- Formulário de Inscrição -->
                <form id="inscricaoForm" class="space-y-6">
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">Nome Completo *</label>
                        <input 
                            type="text" 
                            name="nome" 
                            required
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors"
                            placeholder="Digite seu nome completo"
                        >
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">Telefone/WhatsApp *</label>
                        <input 
                            type="tel" 
                            name="telefone" 
                            required
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors"
                            placeholder="(00) 00000-0000"
                        >
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">E-mail *</label>
                        <input 
                            type="email" 
                            name="email" 
                            required
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors"
                            placeholder="seu@email.com"
                        >
                    </div>
                    
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-3">Curso de Interesse *</label>
                        <select 
                            name="curso" 
                            required
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:outline-none transition-colors"
                        >
                            <option value="">Selecione um curso</option>
                            <option value="1-basico">1º Básico em Teologia</option>
                            <option value="2-basico">2º Básico em Teologia</option>
                            <option value="medio">Médio em Teologia</option>
                            <option value="diaconos">Curso para Diáconos e Auxiliares</option>
                        </select>
                    </div>
                    
                    <div class="bg-blue-50 rounded-xl p-4">
                        <h4 class="font-semibold text-blue-900 mb-2">📞 Próximos Passos:</h4>
                        <p class="text-sm text-blue-700">
                            Após o envio, nossa equipe entrará em contato via WhatsApp em até 24h para fornecer mais informações e auxiliar na matrícula.
                        </p>
                    </div>
                    
                    <div class="flex space-x-3">
                        <button 
                            type="button" 
                            id="cancelarBtn"
                            class="flex-1 border-2 border-gray-200 text-gray-700 py-3 rounded-xl font-semibold hover:bg-gray-50 transition-colors"
                        >
                            Cancelar
                        </button>
                        <button 
                            type="submit" 
                            class="flex-1 btn-primary text-white py-3 rounded-xl font-semibold"
                        >
                            Enviar Inscrição
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script type="module">
        // Scroll reveal animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                }
            });
        }, observerOptions);

        // Observe all scroll-reveal elements
        document.querySelectorAll('.scroll-reveal').forEach(el => {
            observer.observe(el);
        });

        // Initial reveal for elements in viewport
        setTimeout(() => {
            document.querySelectorAll('.scroll-reveal').forEach(el => {
                const rect = el.getBoundingClientRect();
                if (rect.top < window.innerHeight && rect.bottom > 0) {
                    el.classList.add('revealed');
                }
            });
        }, 100);

        // Smooth parallax effect for floating elements
        let ticking = false;

        function updateParallax() {
            const scrolled = window.pageYOffset;
            const parallax = document.querySelectorAll('.floating-element');
            
            parallax.forEach((element, index) => {
                const speed = (index + 1) * 0.3;
                const yPos = -(scrolled * speed);
                element.style.transform = `translateY(${yPos}px)`;
            });
            
            ticking = false;
        }

        function requestTick() {
            if (!ticking) {
                requestAnimationFrame(updateParallax);
                ticking = true;
            }
        }

        window.addEventListener('scroll', requestTick);

        // Form submission
        document.querySelector('form').addEventListener('submit', (e) => {
            e.preventDefault();
            
            // Simple form validation and submission feedback
            const inputs = document.querySelectorAll('input, textarea, select');
            let isValid = true;
            
            inputs.forEach(input => {
                if (input.type === 'email') {
                    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                    if (!emailRegex.test(input.value)) {
                        isValid = false;
                        input.style.borderColor = '#ef4444';
                    } else {
                        input.style.borderColor = '#22c55e';
                    }
                } else if (input.value.trim() === '') {
                    isValid = false;
                    input.style.borderColor = '#ef4444';
                } else {
                    input.style.borderColor = '#22c55e';
                }
            });
            
            if (isValid) {
                // Show success message
                const button = document.querySelector('button[type="submit"]');
                const originalText = button.textContent;
                button.textContent = 'Mensagem Enviada! ✨';
                button.style.background = 'linear-gradient(135deg, #22c55e 0%, #16a34a 100%)';
                
                setTimeout(() => {
                    button.textContent = originalText;
                    button.style.background = 'linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%)';
                    inputs.forEach(input => {
                        input.value = '';
                        input.style.borderColor = '#e5e7eb';
                    });
                }, 3000);
            }
        });

        // Enhanced hover effects
        document.querySelectorAll('.hover-lift').forEach(element => {
            element.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
            });
            
            element.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
            });
        });

        // Navigation scroll effect
        window.addEventListener('scroll', () => {
            const nav = document.querySelector('nav');
            if (window.scrollY > 100) {
                nav.style.background = 'rgba(30, 58, 138, 0.95)';
                nav.style.backdropFilter = 'blur(20px)';
            } else {
                nav.style.background = 'rgba(255, 255, 255, 0.15)';
                nav.style.backdropFilter = 'blur(15px)';
            }
        });

        // Course card click handling
        document.querySelectorAll('.course-card button').forEach(button => {
            button.addEventListener('click', function() {
                const courseName = this.closest('.course-card').querySelector('h3').textContent;
                alert(`Redirecionando para matrícula do curso: ${courseName}`);
            });
        });

        // Smooth scroll navigation
        document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetSection = document.querySelector(targetId);
                
                if (targetSection) {
                    const navHeight = document.querySelector('nav').offsetHeight;
                    const targetPosition = targetSection.offsetTop - navHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Footer navigation links
        document.querySelectorAll('footer a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetSection = document.querySelector(targetId);
                
                if (targetSection) {
                    const navHeight = document.querySelector('nav').offsetHeight;
                    const targetPosition = targetSection.offsetTop - navHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Hero section buttons navigation
        document.querySelector('.btn-primary').addEventListener('click', function() {
            const cursosSection = document.querySelector('#cursos');
            if (cursosSection) {
                const navHeight = document.querySelector('nav').offsetHeight;
                const targetPosition = cursosSection.offsetTop - navHeight - 20;
                
                window.scrollTo({
                    top: targetPosition,
                    behavior: 'smooth'
                });
            }
        });

        // Video Modal functionality
        const videoModal = document.getElementById('videoModal');
        const closeVideoModal = document.getElementById('closeVideoModal');
        const youtubeFrame = document.getElementById('youtubeFrame');
        const videoTitle = document.getElementById('videoTitle');
        const inscrevaSeVideo = document.getElementById('inscrevaSeVideo');
        const videoPreviews = document.querySelectorAll('.video-preview');

        // Abrir modal de vídeo
        videoPreviews.forEach(preview => {
            preview.addEventListener('click', function() {
                const videoId = this.dataset.video;
                const title = this.dataset.title;
                
                videoTitle.textContent = title;
                youtubeFrame.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&rel=0`;
                
                videoModal.classList.remove('hidden');
                videoModal.classList.add('flex');
                document.body.style.overflow = 'hidden';
            });
        });

        // Fechar modal de vídeo
        function fecharVideoModal() {
            videoModal.classList.add('hidden');
            videoModal.classList.remove('flex');
            document.body.style.overflow = 'auto';
            youtubeFrame.src = ''; // Para o vídeo
        }

        closeVideoModal.addEventListener('click', fecharVideoModal);

        // Fechar modal clicando fora
        videoModal.addEventListener('click', function(e) {
            if (e.target === videoModal) {
                fecharVideoModal();
            }
        });

        // Fechar modal com ESC
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && !videoModal.classList.contains('hidden')) {
                fecharVideoModal();
            }
        });

        // Botão inscrever-se no modal de vídeo
        inscrevaSeVideo.addEventListener('click', function() {
            fecharVideoModal();
            // Abrir modal de inscrição
            setTimeout(() => {
                modal.classList.remove('hidden');
                modal.classList.add('flex');
                document.body.style.overflow = 'hidden';
            }, 300);
        });

        // Modal Área do Aluno functionality
        const areaAlunoModal = document.getElementById('areaAlunoModal');
        const areaAlunoBtn = document.getElementById('areaAlunoBtn');
        const closeAreaAlunoModal = document.getElementById('closeAreaAlunoModal');
        const cancelarAreaAluno = document.getElementById('cancelarAreaAluno');

        // Abrir modal área do aluno
        areaAlunoBtn.addEventListener('click', function() {
            areaAlunoModal.classList.remove('hidden');
            areaAlunoModal.classList.add('flex');
            document.body.style.overflow = 'hidden';
        });

        // Fechar modal área do aluno
        function fecharAreaAlunoModal() {
            areaAlunoModal.classList.add('hidden');
            areaAlunoModal.classList.remove('flex');
            document.body.style.overflow = 'auto';
        }

        closeAreaAlunoModal.addEventListener('click', fecharAreaAlunoModal);
        cancelarAreaAluno.addEventListener('click', fecharAreaAlunoModal);

        // Fechar modal clicando fora
        areaAlunoModal.addEventListener('click', function(e) {
            if (e.target === areaAlunoModal) {
                fecharAreaAlunoModal();
            }
        });

        // Fechar modal com ESC
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && !areaAlunoModal.classList.contains('hidden')) {
                fecharAreaAlunoModal();
            }
        });

        // Função para acessar área do aluno
        window.acessarAreaAluno = function(tipo) {
            fecharAreaAlunoModal();
            
            if (tipo === 'ibadejuf') {
			
                // Simular redirecionamento para plataforma IBADEJUF
                /*setTimeout(() => {
                    alert('Redirecionando para a Área do Aluno IBADEJUF...\n\nEm breve você será direcionado para a plataforma de cursos de Teologia.');
                    // window.open('https://plataforma-ibadejuf.com/login', '_blank');
                }, 300);
				*/
				//window.location.href = "http://ibadejuf.com.br/loginv2";
				window.open('http://ibadejuf.com.br/loginv2', '_blank');
            } else if (tipo === 'diaconos') {
                // Simular redirecionamento para plataforma Diáconos
                /*setTimeout(() => {
                    alert('Redirecionando para a Área do Aluno - Curso de Diáconos...\n\nEm breve você será direcionado para a plataforma específica do curso diaconal.');
                    // window.open('https://plataforma-diaconos.com/login', '_blank');
                }, 300);
				*/
				window.open('http://diaconos.ibadejuf.com.br/loginv2', '_blank');
            }
        };

        // Modal de Inscrição functionality
        const modal = document.getElementById('inscricaoModal');
        const inscricaoBtn = document.getElementById('inscricaoBtn');
        const closeModal = document.getElementById('closeModal');
        const cancelarBtn = document.getElementById('cancelarBtn');
        const inscricaoForm = document.getElementById('inscricaoForm');

        // Abrir modal
        inscricaoBtn.addEventListener('click', function() {
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            document.body.style.overflow = 'hidden'; // Impede scroll da página
        });

        // Fechar modal
        function fecharModal() {
            modal.classList.add('hidden');
            modal.classList.remove('flex');
            document.body.style.overflow = 'auto'; // Restaura scroll da página
            inscricaoForm.reset(); // Limpa o formulário
        }

        closeModal.addEventListener('click', fecharModal);
        cancelarBtn.addEventListener('click', fecharModal);

        // Fechar modal clicando fora
        modal.addEventListener('click', function(e) {
            if (e.target === modal) {
                fecharModal();
            }
        });

        // Fechar modal com ESC
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && !modal.classList.contains('hidden')) {
                fecharModal();
            }
        });

        // Submissão do formulário de inscrição
        inscricaoForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            const formData = new FormData(inscricaoForm);
            const dados = {};
            
            // Coleta os dados do formulário
            for (let [key, value] of formData.entries()) {
                dados[key] = value;
            }
            
            // Validação básica
            let isValid = true;
            const inputs = inscricaoForm.querySelectorAll('input[required], select[required]');
            
            inputs.forEach(input => {
                if (!input.value.trim()) {
                    isValid = false;
                    input.style.borderColor = '#ef4444';
                } else if (input.type === 'email') {
                    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                    if (!emailRegex.test(input.value)) {
                        isValid = false;
                        input.style.borderColor = '#ef4444';
                    } else {
                        input.style.borderColor = '#22c55e';
                    }
                } else {
                    input.style.borderColor = '#22c55e';
                }
            });
            
            if (isValid) {
                // Simular envio
                const submitBtn = inscricaoForm.querySelector('button[type="submit"]');
                const originalText = submitBtn.textContent;
                
                submitBtn.textContent = 'Enviando...';
                submitBtn.disabled = true;
                
                setTimeout(() => {
                    submitBtn.textContent = 'Inscrição Enviada! ✨';
                    submitBtn.style.background = 'linear-gradient(135deg, #22c55e 0%, #16a34a 100%)';
                    
                    setTimeout(() => {
                        // Exibir mensagem de sucesso
                        alert(`Obrigado, ${dados.nome}! Sua inscrição foi enviada com sucesso. Nossa equipe entrará em contato via WhatsApp em breve!`);
                        
                        // Resetar e fechar modal
                        submitBtn.textContent = originalText;
                        submitBtn.style.background = 'linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%)';
                        submitBtn.disabled = false;
                        
                        // Resetar cores dos campos
                        inputs.forEach(input => {
                            input.style.borderColor = '#e5e7eb';
                        });
                        
                        fecharModal();
                    }, 2000);
                }, 1000);
            }
        });

        // Formatação do telefone
        const telefoneInput = document.querySelector('input[name="telefone"]');
        telefoneInput.addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            
            if (value.length <= 11) {
                if (value.length <= 2) {
                    value = value.replace(/(\d{2})/, '($1');
                } else if (value.length <= 7) {
                    value = value.replace(/(\d{2})(\d{4,5})/, '($1) $2');
                } else {
                    value = value.replace(/(\d{2})(\d{5})(\d{4})/, '($1) $2-$3');
                }
            }
            
            e.target.value = value;
        });
    </script>
</body>
</html>